void main ( ) {
  foo ( );
  // semanitcally bad, but gramatically ok
}
void foo ( ) {
}
int func1 ( int a ) {
  int b;
  while ( a > 0 || a < 5 ) {
    //Makes no sense, but gramatically ok
    b = 45 + ( !!!true );
  }
  return 0x45;
  return ( 0xBB + 3 );
}
